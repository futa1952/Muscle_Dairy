<%= form_with(model: calender, local: true) do |form| %>
<% if calender.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(calender.errors.count, "error") %> prohibited this post from being saved:</h2>

  <ul>
    <% calender.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :title %>
  <%= form.text_field :title %>
</div>

<div class="field">
  <%= form.label :body %>
  <%= form.text_area :body %>
</div>

<div class="field">
  <%= form.label :start %>
  <%= form.datetime_select :start %>
</div>
<div class="field">
  <%= form.label :end %>
  <%= form.datetime_select :end %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>